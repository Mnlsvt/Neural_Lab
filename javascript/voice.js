function voice_help1(){
    window.speechSynthesis.speak(new SpeechSynthesisUtterance('If you want to zoom in to the page, you can click the zoom in button on the left.'));
}

function voice_help2(){
    window.speechSynthesis.speak(new SpeechSynthesisUtterance('If you want to zoom out of the page, you can click the zoom out button on the left.'));
}
